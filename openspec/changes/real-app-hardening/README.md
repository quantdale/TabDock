# real-app-hardening

Final hardening campaign in the current qualification sequence. Closes predecessor real-app rows 19.1 (browser F11 visual) and 19.4 (adopted-app privacy) and qualifies Chromium, Windows 11 Notepad, and Windows Terminal under the Shepherd/no-reparent contract.

See `proposal.md`, `design.md`, `tasks.md` and `specs/` deltas. Validate with:

```
openspec validate real-app-hardening --type change --strict --no-interactive --json
```
