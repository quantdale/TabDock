## 1. Remove or rewrite the generated workflow

- [x] 1.1 Delete `.github/workflows/copilot-setup-steps.yml` as generated, OR rewrite it so every `actions/*` use is a 40-character SHA with a `# vX` comment, every checkout sets `persist-credentials: false`, OpenSpec is installed from `tools/openspec` with `npm ci --ignore-scripts` (no `npm install -g`), and the runner is `windows-latest` if the job is expected to build TabDock. Default is delete. Decision: **delete** — the file was generated by an OpenSpec 1.9 CLI run, ran on `ubuntu-latest` (cannot build `net8.0-windows`), and installed an unpinned global CLI. Remaining workflows: `build.yml`, `prepare-release-candidate.yml`, `publish-release.yml`, `qualify-candidate.yml`.
- [x] 1.2 If the workflow is deleted, remove `githubCopilot.cloudAgent` from `openspec/config.yaml`. If it is rewritten and compliant, keep the key only when it matches the rewritten workflow. Do not leave `cloudAgent: true` pointing at a missing or Ubuntu/global-install job. Removed the two-line `githubCopilot:` block; `openspec/config.yaml` is back to its pinned-1.8.0 shape.

## 2. Close the allowlist hole

- [x] 2.1 In `scripts/release-tooling-tests.ps1`, change `all-actions-pinned-to-immutable-shas` to enumerate every `.github/workflows/*.yml` via `Get-ChildItem` (same pattern as `workflow-run-blocks-contain-no-workflow-dispatch-expressions`). A file containing `uses: actions/[^@]+@v\d` MUST fail. Keep the expected SHA map for the known actions. Implemented with `Get-ChildItem -Filter '*.yml' | Sort-Object Name` plus a `Count -ge 4` anti-vacuity guard; the `$expected` SHA map is unchanged.
- [x] 2.2 Change `release-workflow-checkouts-have-no-unnecessary-persisted-credentials` to the same full-directory enumeration. Every checkout in every workflow MUST contain `persist-credentials: false`; no workflow MAY contain `persist-credentials: true`. Implemented; kept a total-checkout anti-vacuity guard (`$checkoutCount -ge 4`) so a broken extractor cannot pass silently, without requiring a future workflow to contain a checkout.

## 3. Specs

- [x] 3.1 Apply the `ci-tooling` delta (hosted OpenSpec install in any workflow). Added `### Requirement: Every hosted OpenSpec install SHALL use the repository-owned pin` (with both delta scenarios) to `openspec/specs/ci-tooling/spec.md`.
- [x] 3.2 Apply the `release-engineering` ADDED pin requirement and the MODIFIED persist-credentials requirement (full block, all workflows). Added `### Requirement: Every hosted workflow SHALL pin actions to immutable SHAs` and replaced `### Requirement: Release checkouts do not persist credentials` with the MODIFIED full-workflow block (all workflows, generated setup included) in `openspec/specs/release-engineering/spec.md`.

## 4. Verification

- [x] 4.1 Run `pwsh -File scripts/release-tooling-tests.ps1`. It MUST fail while the generated Ubuntu/`checkout@v4`/`npm install -g` file is present, and MUST pass (179+ cases) after delete-or-rewrite plus the allowlist expansion.
  - RED evidence (file present, post-expansion): `177 passed, 2 FAILED` — `all-actions-pinned-to-immutable-shas : mutable actions/* tag remains in copilot-setup-steps.yml` and `release-workflow-checkouts-have-no-unnecessary-persisted-credentials : checkout in copilot-setup-steps.yml must not persist credentials`.
  - GREEN evidence (file deleted, config key removed): `179 passed, 0 failed` (exit 0); `release-workflow-checkouts-have-no-unnecessary-persisted-credentials` and `all-actions-pinned-to-immutable-shas` both PASS. Expanded gates and `--no-interactive` OpenSpec validation (`--changes --strict`) pass.
- [x] 4.2 Confirm `build.yml` / Stage A / Stage B pins are unchanged. No workflow file other than the deleted generated file was touched (`git status` shows only the deletion of the untracked generated file); the unchanged `$expected` SHA map in the pin test still passes, and the DigiCert Stage A pin assertion is untouched.
- [x] 4.3 Run `openspec validate close-generated-workflow-trust-hole --strict` and `openspec validate --changes --strict`.
