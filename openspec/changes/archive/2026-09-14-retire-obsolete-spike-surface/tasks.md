## 1. Specs

- [x] 1.1 Apply the `test-tooling-safety` delta: delete the three Spike requirements from `openspec/specs/test-tooling-safety/spec.md` and add the live-tooling requirement from this change. Rewrite that spec's `## Purpose` to describe ValidationDriver ownership/spawn/isolation/shard/input-identity safety only (50+ characters, no Spike as a live target). Deleted the three Spike requirement blocks (reparent ownership, guarded-spawn routes, child-mode HWND/PID validation); added `### Requirement: Live tooling SHALL NOT present the deleted Spike as current` with its three scenarios; Purpose now reads "ValidationDriver ownership, spawn, isolation, shard, and input-identity safety for real-input qualification. The experimental reparent Spike is not a live target."
- [x] 1.2 Apply the `ci-tooling` delta: add the in-repo-only performance-matrix requirement to `openspec/specs/ci-tooling/spec.md`. Added `### Requirement: Performance build matrix SHALL compile only in-repo projects` with its two scenarios (appended after the compile-only Performance requirement).
- [x] 1.3 Replace each archive-stub `## Purpose` (`TBD - created by archiving…`) with a durable summary of 50+ characters, editing only the Purpose section of: `ci-tooling`, `diagnostic-privacy`, `monitor-dpi-probing`, `monitor-health-policy`, `native-window-identity`, `native-window-qualification`, `production-diagnostics`, `recovery-concurrency-closure`, `recovery-journal-compatibility`, `showwindow-poststate`, `startup-group-visibility`, `visual-performance-requalification`, `visual-qualification-evidence`. Do not rewrite requirement bodies. All 13 replaced with the design-table summaries; requirement bodies untouched.

## 2. Scripts

- [x] 2.1 In `scripts/perf.ps1`, remove the `build-spike-*` matrix step and any `Spike\TabDock.Spike\TabDock.Spike.csproj` path. Keep restore/build of the solution, ValidationDriver, GuineaPig, and Performance projects. Removed the 3-line `build-spike-$Configuration` measurement; solution/app/driver/GuineaPig/Performance steps remain.
- [x] 2.2 In `scripts/validate.ps1`, rewrite the synopsis so it no longer claims the solution build covers Spike. Do not add a Spike build. Synopsis now says the solution build covers "the main app and UnitTests" plus the ValidationDriver, GuineaPig, and compile-only Performance projects.
- [x] 2.3 In `TabDock.csproj` `DefaultItemExcludes`, keep `Spike/**` excluded (add it back if missing) so a recreated `Spike/` folder of `.cs` files cannot be globbed into the product WPF project. Added back: `$(DefaultItemExcludes);bin/**;obj/**;tests/**;docs/**;Spike/**`, with the guard reason in the adjacent comment.

## 3. Canonical docs

- [x] 3.1 In `AGENTS.md`, remove the claim that the solution contains an experimental Spike. State that `TabDock.sln` contains the app and UnitTests; ValidationDriver/GuineaPig/Performance are built by project path. Rewritten.
- [x] 3.2 In `docs/TESTING.md`, remove Spike from the `validate.ps1` build list. Now "solution/app/driver/GuineaPig/Performance Release builds".
- [x] 3.3 In `docs/internal/AGENT_GUIDE.md`, replace the solution-structure tree that lists `Spike/TabDock.Spike`, the `DefaultItemExcludes` Spike glob, and the "builds only the main app and the Spike" note with the current layout. Keep `docs/internal/guarded-spawn-pattern.md` as historical provenance; do not instruct readers to build Spike. Update the `HotkeyService` one-liner so it also names diagnostic `Ctrl+Alt+Shift+D` and tab-navigation `Ctrl+Alt+PageUp/PageDown`. Tree now lists `tests/UnitTests/` and a `tests/Performance/` row; glob note documents `Spike/**` as a recreated-tree guard; build note names UnitTests; the "Survival spike" section is gone; the runaway-recursion reference is past tense; HotkeyService row names all three global hotkey families.
- [x] 3.4 In `README.md`, replace the hosted-CI sentence that names `PresentationOperationBudget` with the live split unit surfaces (`SplitInteractionPolicy`, `SplitPresentationController`, and the Wave 3 transition tests). Do not resurrect the budget sink. Now names `SplitInteractionPolicy`, `SplitPresentationController`, and `SplitControllerTransitionBehaviorTests`/related Wave 3 tests.

## 4. Verification

- [x] 4.1 `rg` the live surfaces (`AGENTS.md`, `docs/TESTING.md`, `docs/internal/AGENT_GUIDE.md`, `scripts/validate.ps1`, `scripts/perf.ps1`, `openspec/specs/test-tooling-safety`) and confirm none still present Spike as a current build/solution/safety target. Historical `docs/audits/` and archived changes MAY still mention it. `AGENTS.md`, `docs/TESTING.md`, `scripts/validate.ps1`, and `scripts/perf.ps1` have zero matches; `AGENT_GUIDE.md` retains only the `Spike/**` exclusion guard and a past-tense provenance sentence; the spec retains only the new anti-obligation requirement and Purpose.
- [x] 4.2 Confirm `scripts/perf.ps1` no longer contains `TabDock.Spike`. Do not run the full performance matrix unless requested; the missing-project compile is the defect being removed. Zero matches for `Spike` in `scripts/perf.ps1`; matrix not run.
- [x] 4.3 Run `openspec validate retire-obsolete-spike-surface --strict` and `openspec validate --all --strict` (or `--all --no-interactive` on the pinned 1.8.0 CLI). All 38 main specs plus both active changes MUST pass. Purpose lines MUST be 50+ characters. Pinned CLI 1.8.0: `retire-obsolete-spike-surface` valid; ` --all --strict --no-interactive` = 41 passed, 0 failed (38 specs + 3 changes).
- [x] 4.4 Run `dotnet build TabDock.sln -c Release` to confirm the solution still has no Spike project and still builds. Build succeeded, 0 warnings, 0 errors; compiled projects are only `TabDock` and `TabDock.UnitTests`.
