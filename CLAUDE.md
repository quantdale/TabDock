# Claude Code adapter

Use the root `AGENTS.md` as the canonical repository instructions; do not maintain a second copy here.

At session start, read `.agent/STATE.md` and any plan it references. Use `.agent/README.md` and the relevant workflow under `.agent/workflows/` for durable execution and checkpoints.

For broad repository discovery, use the project-local Repowise MCP registration in `.mcp.json` when available, then verify retrieved locations against source. Load `docs/internal/AGENT_GUIDE.md`, `docs/TESTING.md`, or other project references only when the task requires their detail.
