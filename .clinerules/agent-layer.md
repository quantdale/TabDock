# Shared repository agent layer

Read the root `AGENTS.md` as the canonical repository instructions, then `.agent/STATE.md` and any referenced plan. Use `.agent/README.md` and its workflows for durable checkpoints. Prefer the project’s lean Repowise MCP for broad discovery when available, and verify retrieved locations against actual source before editing. Do not duplicate repository rules in this adapter.
